# 区域销售分析看板 — 产品 Spec（审批演示）

## 1. 背景
- 区域销售数据分散在 CRM、ERP、手工 Excel 中，管理层无法按区域/渠道/时间快速对比业绩。
- 现有报表 T+3 延迟，口径不一致（订单口径 vs 回款口径），难以支撑周会决策。
- 需要统一看板，实现 T+1 自动更新，明确唯一数据口径。

## 2. 目标
- G1：实现全国 6 大区、32 省销售业绩一屏可视。
- G2：核心指标 T+1 更新，查询响应 < 3s（P90）。
- G3：统一指标口径，差异解释可追溯。
- 非目标：暂不做预测、暂不做移动端原生 App。

## 3. 范围
**In Scope：**
- 区域/省份/城市三级下钻，时间维度（日/周/月/季/年）。
- 核心指标：销售额、订单量、客单价、退货率、回款额、同比/环比。
- 权限：按大区隔离，管理层看全国。
- 导出 CSV、订阅邮件周报。

**Out of Scope：**
- 销售预测、库存联动、绩效考核自动算薪。

## 4. 功能需求
- F1 看板首页：KPI 卡 + 趋势图 + 区域地图 + Top10 门店榜。
  - 支持筛选：时间、区域、渠道（线上/线下/经销）、品类。
- F2 下钻分析：点击大区 -> 省 -> 城市，联动所有图表。
- F3 对比分析：同比/环比自动计算，异常波动标红（>±15%）。
- F4 明细查询：订单明细表，支持分页（50/页）、排序、导出。
- F5 订阅推送：每周一 9:00 自动发送上周战报 PDF 到邮箱。

## 5. 数据口径
| 指标 | 口径定义 | 数据源 | 更新频率 |
|---|---|---|---|
| 销售额 | 已审核订单金额（含税），剔除取消/关闭，退货单独扣减 | ERP.sales_order | T+1 02:00 |
| 订单量 | 已审核订单数，去重 order_id | ERP.sales_order | T+1 02:00 |
| 客单价 | 销售额 / 订单量 | 计算 | 随看板实时 |
| 退货率 | 退货金额 / 销售额 | ERP.return_order | T+1 02:00 |
| 回款额 | 实际到账金额 | CRM.receipt | T+1 03:00 |
| 同比/环比 | 对应周期对比，需满周期才显示 | 计算 | 实时 |

- 时区：Asia/Shanghai，日切点 00:00。
- 缺失值：无数据展示“-”，不计入 0。
- 口径冲突：以本 Spec 为准，旧报表标注“待下线”。

## 6. 验收标准
- A1：筛选任意大区+月份，KPI 与 SQL 复核误差 < 0.1%。
- A2：P90 查询 < 3s（100 万订单量级测试集）。
- A3：权限测试：华东用户无法访问华南数据。
- A4：T+1 任务连续 7 天成功率 100%，失败自动告警。
- A5：导出 CSV 字段与明细表一致，UTF-8 with BOM。
- A6：周报邮件 100% 送达测试邮箱，PDF 排版无错位。

## 7. 实施步骤
1. 数据接入：确认 ERP/CRM 表结构，建 ODS 层（2 天）。
2. 数仓建模：DWD/DWS 宽表 + 指标字典（3 天）。
3. 看板开发：KPI+地图+趋势+明细（4 天）。
4. 权限+推送+导出（2 天）。
5. 联调验收：UAT + 权限 + 性能测试（2 天）。
6. 上线：灰度 1 个大区 -> 全量，配监控告警。

---
> 本文件为审批演示用 Spec，等待审批决定后才生成实施计划。
