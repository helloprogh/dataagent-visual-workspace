# 区域销售分析看板 产品 Spec

## 1. 背景
- 销售团队按大区/省/城市三级管理，现有数据分散在 CRM、ERP、Excel，口径不一致。
- 管理层无法快速对比区域业绩、定位低效区域，月度复盘准备耗时 > 2 人日。
- 需要统一的区域销售分析看板，支撑目标管理与资源调配。

## 2. 目标
- G1：统一区域销售数据口径，提供一站式查看入口。
- G2：支持大区/省/城市下钻，对比销售额、订单量、客单价、同比/环比。
- G3：识别 TOP/底部门店与异常波动，输出可执行洞察。
- 指标：月报准备时间 < 0.5 人日；核心指标口径一致率 100%；看板日活覆盖 90% 销售管理者。

## 3. 范围
**在范围内 (MVP)：**
- 区域层级：大区（华东/华南/华北/华西）→ 省 → 城市。
- 核心指标：销售额、订单量、客单价、退货率、同比、环比、目标完成率。
- 组件：MetricCard（KPI）、BarChart（区域对比）、LineChart（趋势）、DataTable（明细）、PieChart（品类占比）、InsightCard/WarningCard。
- 筛选：时间范围（近7/30/90天/自定义）、区域、品类、渠道。
- 权限：按区域角色隔离数据。

**不在范围内：**
- 单品毛利/成本核算、预测模型、移动端原生 App、自动调价策略。

## 4. 功能需求
### 4.1 总览 KPI 区
- 4 个 MetricCard：本期销售额、订单量、目标完成率、同比增长。
- 支持时间筛选联动，显示环比箭头与异常 Badge。

### 4.2 区域对比
- BarChart：各大区销售额对比，支持切换订单量/完成率。
- 点击柱形下钻到省/城市。
- DataTable：区域明细，列 = 区域/销售额/订单/客单/同比/环比/完成率/退货率，支持排序导出。

### 4.3 趋势与结构
- LineChart：所选区域销售额趋势（按日/周/月聚合）。
- PieChart：品类/渠道占比。

### 4.4 洞察与预警
- InsightCard：自动生成 TOP3/BOTTOM3 区域解读。
- WarningCard：触发规则如完成率 < 70%、环比下滑 > 20%、退货率 > 5%。

### 4.5 交互
- Row/Column 布局，Tabs 切换“对比/趋势/明细”。
- 筛选器变更 < 1s 刷新，图表联动。

## 5. 数据口径
| 指标 | 口径定义 | 来源 | 更新频率 |
|---|---|---|---|
| 销售额 | 已支付订单实付金额（含税，不含退款），退款在退款日冲减 | ERP订单表 `paid_amount` | T+1 每日 6:00 |
| 订单量 | 已支付且未全额退款订单数 | ERP订单表 | T+1 |
| 客单价 | 销售额 / 订单量 | 计算 | 随看板 |
| 退货率 | 退货订单数 / 总订单数 | CRM售后表 | T+1 |
| 同比/环比 | vs 去年同期 / 上一周期 | 计算 | 随看板 |
| 目标完成率 | 销售额 / 销售目标，目标来自年度目标表按月拆分 | 目标表 `sales_target` | 每月1日 |
| 区域归属 | 按客户收货省市映射到大区，缺失映射归为“待分配” | 区域维度表 | 变更即更新 |

- 时区：UTC+8；货币：CNY；小数：金额保留2位，百分比保留1位。
- 排除：测试订单、内部调拨单、金额 <= 0 的脏数据。

## 6. 验收标准
- [ ] A1：筛选大区/时间后，KPI、图表、明细在 1s 内一致刷新。
- [ ] A2：下钻华东→浙江→杭州三级数据求和与大区总额误差 < 0.01%。
- [ ] A3：完成率、同比/环比与手工 SQL 抽查 30 条一致率 100%。
- [ ] A4：权限隔离：华南用户不可见华东明细。
- [ ] A5：预警规则 100% 触发准确，InsightCard 无空值。
- [ ] A6：近90天数据加载 P95 < 2s，导出 CSV 完整。
- [ ] A7：本文件已保存为 `spec-acceptance.md` 并通过审批。

## 7. 实施步骤
1. 数据接入：确认 ERP/CRM/目标表字段，完成 ETL 与区域映射表（2天）。
2. 口径评审：与财务/销售确认第5节口径，冻结 SQL（0.5天）。
3. 看板搭建：KPI→对比→趋势→洞察顺序实现，组件联调（3天）。
4. 权限与性能：行级权限、缓存、P95 优化（1天）。
5. UAT 验收：按第6节逐项验收，修复回归（1天）。
6. 上线：发布、培训文档、运营移交。后续实现需审批通过后启动。
