# UI 持续重构进度

目标：全部生成式 UI 统一 A2UI，AG-UI 保持运行及审批真源；独立迭代验证后提交并推送。

## 迭代路线

1. 恢复浏览器测试、依赖锁定、统一 CI，修复测试暴露的功能退化。
2. 状态正确性：模型隔离与失败回滚、分页竞争、审批 schema、A2UI busy、预览限流。
3. Conversation 展示层：滚动、面板、输入区、展示模型和预览数据源。
4. A2UI 统一：Catalog 契约、业务卡片、动作路由、历史兼容及回放验证。
5. 会话和产品体验：重命名、搜索、警告、主题字体、上传状态。
6. 性能基线及扩展：资源加载、长会话、Agent Runs 和文件预览能力按后端契约推进。

## 迭代 1（已提交推送：a21532d，远端 CI 成功）

- 将 Playwright 1.62.1 纳入 workspace 依赖和锁文件，增加 `test:e2e`、`check:ui`。
- 浏览器回归迁移到当前 Hash 路由、Element Plus X 输入及 AG-UI/A2UI 活动；独立测试端口 5187。
- CI 和重构分支验证统一执行浏览器测试，失败上传 trace/截图；去除 CI 自动修改和推送锁文件。
- 修复历史恢复骨架屏结束后未滚动至底部，以及创建会话后未记忆所选模型的问题。
- 本地可用 `PLAYWRIGHT_CHANNEL=msedge` 运行已安装 Edge；CI 默认使用 Playwright Chromium。
- 工作区 data-applications 的其他任务修改不纳入本重构提交。
- 验证：Playwright Chromium 16/16 通过（19.3 秒），本地 Edge 16/16 通过；`npm run check` 的 120 项测试、类型检查、生产构建通过；离线检查及本轮文件 diff 检查通过。
- 构建仍有既有 CSS `:deep` 和分包体积警告。浏览器使用模拟 API/SSE，本轮没有宣称完成真实 OpenCode 联调。

## 迭代 2：模型与分页隔离（已提交推送：33277bc，远端 CI 成功）

- 模型切换失败恢复旧选择；会话切换或卸载后忽略旧模型请求；加载期间切换会话也会加载对应模型。
- 模型初始化从当前有效目录中解析记忆值和默认值。
- 历史分页绑定会话代次及 client，旧分页结果不覆盖新会话消息、cursor、loading 或错误状态。
- 切换会话后旧 Run 的 finally/catch 不覆盖新会话运行及中断恢复状态。
- 新增浏览器用例：模型切换失败回滚、模型加载期间切换会话、旧会话分页晚到后新会话仍可分页。
- 验证：Chromium 19/19 通过（21.9 秒）；`npm run check` 的 120 项测试、类型检查和生产构建通过。
- 下一轮：A2UI 动作 busy、审批 schema 与日期时间、文件预览字节限制；随后继续展示层与统一协议迁移。

## 迭代 3：交互和输入边界（已提交推送：7c794a2，远端 CI 成功）

- A2UI 动作按钮使用真实运行/待审批状态，不再依赖固定六秒计时器；补充立即重复点击保护。
- 审批表单与提交入口共用显式 schema 子集校验；不支持的结构提示并阻止提交，日期时间保留时区。
- 修复多中断快捷按钮仅提交单条答案的问题，统一收集全部答案后提交。
- 普通及 ZIP 文本预览按 UTF-8 字节限制为 1 MiB，支持无 Content-Length 响应，取消多余读取并清除跨文件状态。
- 增加支持范围文档 `APPROVAL-AND-PREVIEW-CONTRACT.md` 和输入边界单元/浏览器回归。
- 验证：`npm run check` 的 127 项测试、类型检查和生产构建通过；Chromium 26/26 通过（28.2 秒）。浏览器使用模拟 API/SSE，不代表真实 OpenCode 联调；既有构建警告仍保留。
- 下一轮继续 Conversation 展示层拆分，随后推进 A2UI 单一渲染入口及历史兼容。

## 迭代 4：会话视口职责拆分（已提交推送：965f9cc，远端 CI 成功）

- 提取 `useConversationScroll`，集中管理流式跟随、跳至最新、触顶分页和视口位置恢复，不复制运行时状态。
- 手动加载旧消息也使用分页高度补偿，避免与自动触顶重复请求。
- 会话切换或卸载使旧分页补偿和排队滚动失效，避免移动另一会话的复用容器。
- 新增 5 项组合函数测试；加强浏览器竞争测试，验证旧分页返回后新会话阅读位置保持不变。
- 验证：132 项单元测试、类型检查和生产构建通过；Chromium 26/26 通过（28.9 秒）。既有 CSS/分包警告保留，真实服务联调仍未覆盖。
- 下一轮：展示派生与面板/输入职责继续拆分，再推进 A2UI 单一渲染入口。

## 迭代 5：展示派生与面板状态（已提交推送：49c3b77，远端 CI 成功）

- 提取 `useConversationPresentation`，统一消息分组、响应占位、审批入口与过程文件关联；不复制消息或审批状态。
- 提取 `useConversationPanels`，以一个面板枚举代替互斥布尔值，保留从交付列表打开预览后返回列表的行为。
- 预览审批回执绑定打开代次、文件和中断，切换/重开/关闭预览或卸载后忽略旧结果。
- 新增 5 项派生/面板行为测试及浏览器面板切换用例；已有源码契约断言迁移至新职责边界。
- 验证：137 项单元测试、类型检查和生产构建通过；Chromium 27/27 通过（30.0 秒）。既有构建警告保留；浏览器仍为模拟接口测试。
- 下一轮：头部、输入和 Inspector 组件边界，再继续 A2UI 统一渲染与旧历史适配。

## 迭代 6：头部与 Inspector 组件边界（已提交推送：3106764，远端 CI 成功）

- 提取 `ConversationHeader`，迁移身份、状态、面板操作与头部响应式样式；以 props/events 连接编排层。
- 提取无额外容器的 `ConversationInspector`，保留预览优先级、返回列表和窄屏覆盖布局；不引入运行时状态副本。
- 增加面板按钮 aria-pressed 回归，以及 390px 窄屏按钮尺寸、边界和面板覆盖测试；等待入场动画稳定后测量。
- 验证：137 项单元测试、类型检查、生产构建通过；Chromium 28/28 通过（31.1 秒）。构建既有警告保留，真实 OpenCode 联调仍待覆盖。
- 下一轮：输入区与快捷键职责，之后推进 A2UI 统一协议和历史兼容。

## 迭代 7：输入组件与聚焦边界（已提交推送：0619acd，远端 CI 成功）

- 提取 `ConversationComposer`，封装 XSender、模型选择、文件选择器及附件展示与样式；发送、上传和运行状态仍由原编排层持有。
- 父组件通过类型化 getText/clear/setText/focus 接口处理模板提示、快捷键和发送后清空，不再直接访问输入库实例。
- 修复光标接口恢复选区但未恢复 DOM 焦点的情况，聚焦补偿仅作用于当前可编辑输入框。
- 新增模板填入、斜杠聚焦/正常输入、会话切换清空、本地附件选择与移除回归；原发送和上传回归继续覆盖接线。
- 验证：137 项单元测试、类型检查及生产构建通过；Chromium 30/30 通过（36.2 秒）。既有构建警告保留；尚不代表真实 OpenCode 联调。
- 下一轮进入 A2UI 单一渲染入口与旧历史适配，继续保持 AG-UI 为运行和审批状态真源。

## 迭代 8：A2UI 共享 Catalog 契约（已提交推送：9841b69，远端 CI 成功）

- 统一协议版本、Catalog ID 与组件名白名单到 `shared/a2ui-catalog.mjs`，消除前后端重复声明。
- 将业务实现迁至 `businessCatalog.ts`；注册入口显式合并/覆盖组件，并检查缺失或未声明实现。
- 增加服务端白名单/注册缺失检测，以及浏览器加载真实 Vue Catalog 后与能力声明比对的回归。
- 验证：139 项单元测试、类型检查、生产构建通过；Chromium 31/31 通过（35.1 秒）。既有构建警告保留，浏览器使用模拟 API。
- 本轮是统一迁移的契约基础，旧 `dataagent.ui` 渲染器尚未移除。下一轮处理旧内容适配、ArtifactCard 数据引用和本地/Agent/审批动作分流。

## 迭代 9：旧生成式卡片迁入 A2UI（已提交推送：553166c，远端 CI 成功）

- 新增只读 `legacyUiToA2ui`，映射文本、Markdown、指标、表格和文件；保留原始历史快照，不静默截断合法最大指标集合。
- 新旧 activity 共用 `A2uiSurfaceCard`；删除旧 `GenerativeUiCard.vue` 渲染器（可从 Git 历史恢复）。
- 注册 ArtifactCard，以 artifactId 引用独立文件表；局部预览不触发 Agent Run，确认/取消仍经过现有 AG-UI 审批校验。
- 增加全部类型转换、非法文件 URL、删除状态、跨消息文件 ID 的单元验证，以及浏览器历史重载、文件预览、审批恢复和会话切换回归。
- 验证：142 项单元测试、类型检查和生产构建通过；Chromium 33/33 通过（37.9 秒）。浏览器使用模拟接口，真实 OpenCode 联调未覆盖；既有构建警告保留。
- 后续仍需迁移工具生成文件的直接卡片、补齐原生 A2UI artifact 数据与生产端契约，再完成回放和性能验证；不能把本轮视为全部生成式 UI 统一完成。

## 迭代 10：工具生成文件统一渲染（已提交推送：b9cc045，远端 CI 成功）

- AgentChat 的工具生成文件入口改为 `A2uiArtifactCard`，通过 A2UI ArtifactCard 节点和独立 artifacts 表渲染，不再直接调用底层文件卡。
- 同一 Surface 入口支持紧凑文件展示，避免重复标题和外框；底层文件 UI 仅由 Catalog 引用。
- 新增浏览器历史回归：两次成功写入保留 v1/v2、失败写入不产生文件、预览不启动 Run、最新文件审批经 AG-UI 恢复、成功删除后重载不恢复卡片。
- 验证：142 项单元测试、类型检查与生产构建通过；Chromium 34/34 通过（38.7 秒）。既有构建警告及真实 OpenCode 联调缺口仍保留。
- 下一轮补齐原生 A2UI artifact 数据及生产端契约，再继续回放覆盖、会话体验与性能验证。

## 迭代 11：原生 artifact 生产端契约（验证完成）

- `render_a2ui` 增加独立 artifacts 表；共享规范化校验 ID、数量、文件 URL 和组件引用，抛弃模型声称的审批信息。
- A2uiStream 发布、接收、恢复和删除快照包含同一文件表；现有 SessionRegistry 原样保存快照，无需修改历史存储格式。
- 原生 Surface 与交付列表共享规范化数据，消息级文件 ID 隔离；原生文件不推断审批关联，保留真实 AG-UI 表单入口。
- 新增规范化及快照生命周期单元测试；浏览器直接使用生产端生成的快照，覆盖预览、交付列表、重载、删除与审批权限边界。
- 契约与示例见 `A2UI-ARTIFACT-CONTRACT.md`。后续继续完善回放边界、会话体验、性能基线与真实服务联调。
- 验证：144 项单元测试、类型检查、生产构建通过；Chromium 35/35 通过（40.2 秒）。既有构建警告保留，浏览器仍使用模拟 API，不代表真实 OpenCode 联调。

## 验证规则

每轮执行对应行为测试、类型检查、构建及差异检查。协议或公共运行时变化执行 Adapter 回归；记录实际测试环境，不将 mock 浏览器测试称为真实 OpenCode 联调。

Windows 本地示例：

```powershell
$env:PLAYWRIGHT_CHANNEL = 'msedge'
$env:NO_PROXY = '127.0.0.1,localhost'
npm run test:e2e
npm run check
```

每轮只暂存明确属于本迭代的文件。推送前核对暂存差异，推送后验证远端提交。
