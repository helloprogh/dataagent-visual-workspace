<script setup lang="ts">
defineProps<{title?:string;items?:Array<{id?:string;time?:string;agent:string;message:string;status?:'info'|'running'|'success'|'warning'|'error';meta?:string}>}>()
</script>
<template><section class="gen-card agent-activity-card"><div class="gen-title-row agent-module-head"><div><span class="eyebrow">实时活动</span><span class="gen-title">{{ title || 'Agent 活动流' }}</span></div><span class="activity-live"><i></i>实时</span></div><div class="agent-activity-list"><div v-for="(item,index) in items || []" :key="item.id || `${item.agent}-${index}`" class="agent-activity-item" :class="item.status || 'info'"><time>{{ item.time || '--:--:--' }}</time><div class="activity-rail"><i></i><span v-if="index < (items || []).length - 1"></span></div><div class="activity-copy"><div><b>{{ item.agent }}</b><small v-if="item.meta">{{ item.meta }}</small></div><p>{{ item.message }}</p></div></div></div></section></template>
