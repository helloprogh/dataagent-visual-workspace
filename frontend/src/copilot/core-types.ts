// @copilotkit/core is intentionally part of the app-level API surface.
// Use its framework-agnostic types when building extensions outside Vue components.
import type { FrontendTool } from '@copilotkit/core'

export type AppFrontendTool = FrontendTool<any>
