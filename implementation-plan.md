# 区域销售分析看板 — 实施计划

> 前置：spec-demo.md 已批准通过。

## 1. 里程碑
- M1 数据层（W1）：订单宽表 + 区域维度 + ETL T+1 调度
- M2 服务层（W2）：聚合 API + 权限 + 导出接口
- M3 展示层（W3）：5 模块看板 + 预警 + 联调
- M4 上线（W4 前半）：对账、性能优化、发布

## 2. 任务拆解
1. **数据建模（3d）**
   - 建 `dws_sales_order_wide`，统一口径：不含税、不含退货
   - 建 `dim_region`（区域/省份/城市/销售归属），处理待认领
   - 输出：表结构文档 + 字典
2. **ETL（4d）**
   - 每日 02:00 调度，支持重跑指定日期
   - 对账任务：与财务月报差异 <0.5% 告警
3. **后端（4d）**
   - `GET /api/sales/overview` `GET /api/sales/compare` `GET /api/sales/trend` `GET /api/sales/top` `GET /api/targets`
   - 区域权限拦截器 + P90 <2s（加索引/预聚合）
4. **前端（6d）**
   - 筛选器 + KPI 卡 + 条形/折线/榜单 + 目标预警（<80% 红）
   - CSV 导出与页面一致性校验
5. **测试验收（3d）**
   - 用例：权限隔离、预警准确率 100%、导出一致、性能压测 5w 订单

## 3. 风险与对策
- 口径分歧 → 以第5节数据口径为准，每日对账
- 区域映射缺失 → 待认领清单 + 运营认领流程
- 性能风险 → 预聚合表 + 缓存热点查询

## 4. 交付物
- implementation-plan.md（本文件）
- API 文档、表结构文档、验收报告

## 5. 下一步
- 启动 M1 数据建模评审
