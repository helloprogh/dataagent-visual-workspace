# 区域销售分析看板 — 产品 Spec（草案）

## 1. 背景
区域销售数据分散在 ERP、CRM 和手工台账中，管理层无法按区域、渠道、时间维度快速对比业绩，月度复盘依赖人工拉数，耗时且口径不一致。

## 2. 目标
- 提供统一的区域销售分析看板，支持按区域 / 省份 / 城市 / 销售 / 渠道查看。
- 核心指标 T+1 更新，支持同比、环比、目标完成率分析。
- 将月度复盘准备时间从 2 天缩短至 <2 小时。

## 3. 范围
**在范围内：**
- 销售总览、区域对比、趋势分析、Top 明细、目标管理 5 个模块。
- PC 端 Web 看板，支持导出 CSV。
- 权限按区域隔离。

**不在范围内：**
- 移动端 App、订单级明细钻取、预测算法 v2、自动调价。

## 4. 功能需求
1. **销售总览**：KPI 卡（销售额、订单量、客单价、完成率），支持时间、区域筛选。
2. **区域对比**：条形图 + 表格对比各区域销售额、同比、环比、排名。
3. **趋势分析**：折线图展示日/周/月趋势，支持多区域叠加。
4. **Top 明细**：Top10 产品 / 客户 / 销售员榜单，点击跳转筛选。
5. **目标管理**：导入年度/月度目标，展示完成率与缺口预警（<80% 红色）。

## 5. 数据口径
| 指标 | 口径 | 来源 |
|------|------|------|
| 销售额 | 已审核订单实收金额，不含税，不含退货 | ERP `orders` |
| 订单量 | 已审核订单数，去重退款关闭订单 | ERP `orders` |
| 客单价 | 销售额 / 订单量 | 计算 |
| 同比/环比 | vs 去年同期 / 上一周期，同口径 | 计算 |
| 目标完成率 | 实际销售额 / 分配目标 | CRM + 导入表 |
| 数据更新 | 每日 02:00 调度，T+1 | ETL |

异常规则：缺失区域映射进入“待认领”，退货单独扣减不冲正当期。

## 6. 验收标准
- [ ] 筛选响应 <2s（P90），支持 5 万订单量级。
- [ ] 指标与财务月报差异 <0.5%。
- [ ] 权限隔离验证通过：A 区用户不可见 B 区数据。
- [ ] 导出 CSV 与页面数据一致。
- [ ] 缺口预警准确率 100%（测试用例集）。

## 7. 实施步骤
1. 数据建模：统一订单宽表 + 区域维度表（3 天）。
2. ETL 开发：T+1 调度、重跑机制（4 天）。
3. 后端 API：筛选、聚合、权限接口（4 天）。
4. 前端看板：5 模块 + 图表 + 导出（6 天）。
5. 联调验收：口径对账、性能优化、上线（3 天）。
